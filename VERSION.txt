Configurador MEPLHAA — v1.71
Fecha: 2026-07-19

Cambios desde v1.70:
- Marca TOMZN renombrada a "Tomzn" (patrón de nombres consistente con el resto:
  solo inicial en mayúscula), tanto la categoría como el prefijo de sus 4
  modelos. Fichero: devices.js. Badge -> v1.71.

Configurador MEPLHAA — v1.70
Fecha: 2026-07-19

Cambios desde v1.69:
- Nueva marca TOMZN con 4 magnetotérmicos DIN WiFi (sin config todavía):
  1P 63A TOB8-63WiFi, 2P 63A/80A TOWBC-2P-T, 4P 80A TOWBC-4P-T, 4P 80A
  TOWBC-4P-e. Fichero: devices.js. Badge -> v1.70.

Configurador MEPLHAA — v1.69
Fecha: 2026-07-19

Cambios desde v1.68:
- Asistente: ahora muestra un resumen "GPIOs que usa este dispositivo" (relé/
  LED/botón + modo de cada io) al cargar un dispositivo, igual de claro que el
  formulario avanzado. Antes el asistente no enseñaba los GPIOs de un vistazo.
  Ficheros: app.js (wizardGpioSummaryHTML), style.css.
- Corregido el Shelly 1 Gen3 mal ubicado: la config de ESP32-C3 estaba bajo
  "Shelly 1" (ESP8266) con GPIOs incorrectos. Movida a modelos propios nuevos
  "Shelly 1 Gen3" y "Shelly 1PM Gen3". "Shelly 1" y "Shelly 1PM" quedan solo con
  configs ESP8266. Fichero: devices.js.
- Badge de versión -> v1.69.
(Aparte, no en la web: regenerado el documento Word de configuraciones del
 usuario con un parser mejorado; 251 configs válidas en v12, 19 con errores en
 el original marcadas para revisión manual.)

Configurador MEPLHAA — v1.68
Fecha: 2026-07-19

Cambios desde v1.67:
- Athom +4 (el usuario confirma que son modelos distintos, no duplicados):
  Athom Wall Outlet SK01, Athom 16A EU Plug PG01EU16A, Athom 16A US Plug
  PG03-US16A, Athom 7W 600lm RGBCCT LB017W. Athom pasa a 65 modelos.
  Sin config todavía. Fichero: devices.js. Badge -> v1.68.

Configurador MEPLHAA — v1.67
Fecha: 2026-07-19

Cambios desde v1.66:
- Nueva marca Tongou con 1 modelo: "Tongou 2P 63A Circuit Breaker Power
  Monitoring DIN (TO-Q-SR263JWE)" — magnetotérmico DIN 2P 63A con medidor de
  consumo. Sin config todavía. Fichero: devices.js. Badge -> v1.67.

Configurador MEPLHAA — v1.66
Fecha: 2026-07-19

Cambios desde v1.65:
- Restaurados los dispositivos que se habían quedado fuera al acortar las
  listas en v1.57. Añadidos (sin config todavía): Sonoff +15 (Light BN-SZ01,
  IW100, L1/L1 Lite, S55, SC, B1, NSPanel, RE5V1C, 1 Channel Inching, iFan02/
  03/04-H/04-L, SPM), Shelly +15 (Add-On/Add-on Temp, RGBW2, UNI, Pro 1/1PM/2/
  2PM/3, Plus PM Mini, Duo RGBW 9W/GU10, Vintage A60/G125/ST64), Tuya +9 (AOFO
  C733, Body Fat Scale, CT61W, TY-DIY-S02, Moes UFO-R6, ESP8684, ESP8685,
  Gosund WO1, TOVA), Athom +16 (AR01, KM01, CB01, LS5050C, SD01, LS2812B, EM2,
  EM6, R01/R02/R08, LB01-15W, LB03-12W, LS8P-WLED, LS5P, SK03). Marcas ahora:
  Shelly 41, Sonoff 89, eWeLink 10, Tuya 32, Athom 61, Kincony 14. Total 391.
  Fichero: devices.js. Badge -> v1.66.

Configurador MEPLHAA — v1.65
Fecha: 2026-07-19

Cambios desde v1.64:
- eWeLink completado a 10 modelos: al corregir nombres en v1.57 se habían
  quedado fuera 4. Añadidos (sin config todavía): eWeLink Candle Light 5W
  RGBCCT, eWeLink EU 20A Plug AJW-02_8285, eWeLink Single Wire 1 Gang 2 Way
  No Neutral RF433 (errata "Sinbgle" corregida a "Single"), y LC Technology
  PSF-B04 eWeLink 4 Channel LC-EWL-B04-MB. Fichero: devices.js.

Configurador MEPLHAA — v1.64
Fecha: 2026-07-19

Cambios desde v1.63:
- BUG corregido en "Log output type": los valores de "o" estaban desplazados.
  UART2 (ESP32) generaba "o":4 (que en HAA es Network) y Network generaba
  "o":5 (que es UART0+Network). Corregido según la wiki oficial: UART2=3,
  Network=4, y añadidas las combinaciones que faltaban (UART+Network y
  variantes con buffer, valores 5-11). Fichero: index.html.
- Añadidos 90 ejemplos nuevos al catálogo (de la colección del usuario, ya en
  v12), repartidos entre Sonoff Basic/4CH/4CH Pro/Mini/Dual/Dual R3/Basic R3/
  SV/Touch/T1/TH, Shelly Plus 2PM y Kincony KC868-A4 (garaje, alarmas, timbre,
  persiana, riego, gallinero, piscina, calentador, sensores de presencia...).
  Fichero: devices.js.
- Badge de versión de la interfaz actualizado a v1.64.

Configurador MEPLHAA — v1.63
Fecha: 2026-07-19

Cambios desde v1.62:
- Corregido el número de versión visible en la interfaz: el badge de index.html
  estaba fijo en "v1.49" desde esa versión (nunca se actualizó al subir las
  siguientes). Ahora muestra v1.63 y a partir de ahora se sube junto con
  VERSION.txt en cada release. Fichero: index.html.

Configurador MEPLHAA — v1.62
Fecha: 2026-07-19

Cambios desde v1.61:
- Kincony KC868-A4: añadido 2º ejemplo "Piscina" (aportado por el usuario;
  venía en notación v11 y se convirtió a v12 con el conversor de la app).
  3 sensores DS18B20 (agua/aire/calentador) + 4 relés con botón local (bomba
  principal, calentador con termostato, luces, cascada). Fichero: devices.js.

Cambios desde v1.60:
- Kincony KC868-A4: añadido ejemplo real "Acuario" (aportado por el usuario).
  Placa ESP32: 3 sensores DS18B20 en bus GPIO13 (agua/aire/sump) + 4 relés con
  temporizador (bomba sump GPIO2, aireador GPIO15, luces UV GPIO5 con botón
  GPIO27, osmosis GPIO4 con entrada GPIO36). Reemplaza el placeholder del
  KC868-A4. Fichero: devices.js.

Cambios desde v1.59:
- Eliminada la categoría "Otros" (132 entradas). El catálogo contiene ahora
  SOLO los modelos de las listas facilitadas por el usuario: Shelly, Sonoff,
  eWeLink, Tuya, Athom, Kincony. Las configuraciones antiguas que se
  relacionaron ya quedaron colocadas bajo su modelo nuevo (v1.58); las no
  relacionadas se descartan. Total catálogo 243. (Las configs descartadas
  siguen recuperables desde git v1.51 y los zips.) Fichero: devices.js.

Cambios desde v1.58:
- Nueva categoría Kincony (14 modelos: KC868-A2/A4/A4S/A6/A8/A8M/A8S/A16/A16S/
  A32/A32M/A64/AG/ASR), orden alfanumérico, sin config todavía. Orden de
  fabricantes: Shelly, Sonoff, eWeLink, Tuya, Athom, Kincony, Otros. Total 375.
  Nota: "Otros" aún tiene un "Kincony KC868-A4" que solapa; pendiente de
  reconciliar. Fichero: devices.js.

Cambios desde v1.57:
- Asignadas las configuraciones antiguas a los modelos nuevos según el mapeo
  revisado por el usuario (Excel mapeo-antiguos-nuevos.xlsx): 40 modelos nuevos
  reciben config (94 ejemplos en total); los no relacionados quedan sin ejemplo
  ("Sin configuración aún"). Reparto: Shelly 16 modelos con config, Sonoff 15,
  eWeLink 4, Tuya 1, Athom 4. "Otros" (132) intacto. Total catálogo 361.
  Fichero: devices.js.

Cambios desde v1.56:
- Listados canónicos corregidos: sustituidos los nombres de las 5 marcas por
  la versión limpia y definitiva del usuario (sin códigos de parte largos).
  Shelly 26, Sonoff 74, eWeLink 6, Tuya 23, Athom 45. Ordenados
  alfanuméricamente, sin duplicados, config vacía (pendiente de asignar).
  Se retira el enlace de pinout por dispositivo (los nombres nuevos ya no
  mapean 1:1 con los ficheros de Blakadder; el pinout se añadirá al asignar
  cada config). "Otros" (132) intacto. Total 307. Fichero: devices.js.

Cambios desde v1.55:
- "Mis configuraciones guardadas": los campos Marca y Dispositivo ahora son
  elegir-del-catálogo-o-escribir. Marca: datalist de fabricantes (ya existía).
  Dispositivo: nuevo datalist con los modelos de la marca elegida (editable,
  se puede escribir uno que no esté). Si el dispositivo NO está en el catálogo,
  aparece un aviso "se guardará como pendiente de validar" + botón "Solicitar
  alta en el catálogo" (abre la propuesta pre-rellenada en GitHub, mismo
  mecanismo que "Compartir"). Al guardarlo, la entrada queda marcada como
  pendiente (distintivo "⏳ pendiente de validar" en la lista de guardados).
  Ficheros: index.html, app.js, style.css.

Cambios desde v1.54:
- Quitado el selector de marca/modelo duplicado. El recuadro "¿Sabes qué
  dispositivo es este?" de Configuración General (y del asistente) ya no
  muestra su propio desplegable cuando no hay dispositivo detectado: el único
  selector de dispositivo es el principal de arriba ("Selector de dispositivos
  y funciones"). Cuando hay un dispositivo cargado/detectado, ese recuadro
  sigue mostrando el enlace a su ficha/esquema. Fichero: app.js
  (función renderDeviceHint).

Cambios desde v1.53:
- Nueva categoría con su listado canónico (Blakadder), ordenada
  alfanuméricamente, sin duplicados y SIN configuración todavía (cada modelo
  con su enlace de pinout Tasmota como referencia): Athom (62 modelos).
  Orden de fabricantes: Shelly, Sonoff, eWeLink, Tuya, Athom, Otros.
  Catálogo total 367. Pendiente de asignar configs. Fichero: devices.js.
  Nota: "Otros" aún contiene algún Athom/Tuya/eWeLink que solapa con estas
  categorías nuevas; pendiente de reconciliar más adelante.

Cambios desde v1.52:
- Dos categorías nuevas con su listado canónico (Blakadder), ordenadas
  alfanuméricamente, sin duplicados y SIN configuración todavía (cada modelo
  con su enlace de pinout Tasmota como referencia): eWeLink (10 modelos) y
  Tuya (32 modelos). Orden de fabricantes: Shelly, Sonoff, eWeLink, Tuya,
  Otros. Catálogo total 305. Pendiente de asignar configs. Fichero: devices.js.

Cambios desde v1.51:
- Reorganización de los catálogos Sonoff y Shelly: se sustituyen por el
  listado canónico oficial (nombres de Blakadder), ordenado
  alfanuméricamente y SIN duplicados por mayúsculas/minúsculas. Sonoff pasa
  a 89 modelos y Shelly a 41. Por ahora SIN ejemplos/configuración: cada
  modelo lleva su enlace de pinout (Tasmota) como referencia, pendiente de
  asignarle la configuración MEPLHAA correcta. La categoría "Otros" (132)
  queda intacta. Trabajo en curso: las configs actuales se reasignarán a su
  modelo canónico tras revisión del usuario. Fichero: devices.js.

Cambios desde v1.51:
- Nuevo dispositivo en el catálogo (Otros): "MHCOZY DC 7-32V (interruptor
  1 canal, ESP8285)". Módulo relé/interruptor RF de 1 canal, placa PSF-B01.
  Relé GPIO12, botón GPIO0, LED de estado GPIO13. GPIOs verificados en la
  plantilla Tasmota de Blakadder (mismo pinout que el Sonoff Basic).
  Fichero: devices.js.

Cambios desde v1.49:
- README bilingüe para dar alcance internacional en GitHub. El README.md
  principal pasa a estar en INGLÉS (lo que ve por defecto el visitante del
  repo) y la versión en español se mueve a README.es.md. Ambos llevan un
  selector de idioma arriba ("🇬🇧 English | 🇪🇸 Español"). No cambia nada
  de la app; es solo documentación del repositorio.
- .gitignore: añadido "!/README.es.md" a la lista blanca para que el nuevo
  README en español entre en el repo.

Cambios desde v1.48:
- Idioma inicial automático según el navegador: los navegadores en español
  (es, es-ES, es-419…) arrancan en ESPAÑOL; el resto (inglés, francés,
  italiano, portugués…) arrancan en INGLÉS, para dar más alcance
  internacional sin molestar a la comunidad hispana. El selector ES/EN
  sigue disponible para cambiarlo. Antes arrancaba siempre en español.
  Fichero: i18n.js.
- Añadido .gitignore (lista blanca): el repo solo admite los ficheros
  reales del proyecto, así la basura de 0 bytes que a veces genera el
  shell no puede colarse en un commit.

Cambios desde v1.47:
- Optimización para MÓVIL (el ~70% de las visitas son desde iPhone/Safari,
  según las analíticas). Nueva media query (max-width: 560px) que:
  · Reduce los márgenes/paddings laterales de 32px a 12px (cabecera,
    layout, selector de modo, selector de dispositivos, tarjetas).
  · Apila en una sola columna las rejillas de dos columnas (grid2) y el
    selector de dispositivos.
  · Quita los ancho-mínimo rígidos (tarjetas de elección y selects del
    recuadro "¿Sabes qué dispositivo es este?") para que encojan.
  · Reduce el tamaño del título y añade una red de seguridad contra el
    scroll horizontal.
  Antes había un desbordamiento horizontal (~19 px) en pantallas de móvil;
  ahora el contenido encaja a 375 px sin scroll lateral. El escritorio no
  cambia (la media query solo aplica en pantallas estrechas).
- Fichero: style.css.

Cambios desde v1.46:
- Arreglo: el selector "¿Sabes qué dispositivo es este?" de Configuración
  General arrancaba con la primera marca (Shelly) preseleccionada, lo que
  daba a entender que la web "creía" que el dispositivo era un Shelly.
  Ahora arranca sin marca ("— Elige una marca —") y el usuario la elige;
  hasta que no se elige marca, el desplegable de modelo muestra "— Elige
  primero una marca —" y el botón Guardar no hace nada. No afecta a la
  configuración (ese selector solo muestra el enlace a ficha/esquema).
  Bilingüe ES/EN. Ficheros: app.js, i18n.js.

Cambios desde v1.45:
- Compartir con la comunidad: cada configuración guardada tiene ahora un
  botón "Compartir" que abre una propuesta (issue) pre-rellenada en el
  repositorio de GitHub (haaconfig/meplhaa-configurator), con marca,
  dispositivo, función, descripción, autor y el JSON. Así cualquiera puede
  proponer su MEPLHAA para incorporarlo al catálogo oficial, que ve toda
  la comunidad (tras revisarlo). La configuración se sigue guardando en el
  navegador (localStorage); "Compartir" es el puente hacia el catálogo
  común, manteniendo la web estática (sin servidor) y con control de
  calidad. Bilingüe ES/EN.
- Ficheros: app.js (shareToGithub + botón), i18n.js (textos), style.css
  (la fila de acciones puede envolver con 3 botones). Catálogo sin
  cambios (234).

Cambios desde v1.44:
- "Mis configuraciones guardadas" rediseñado: ahora se guarda como una
  entrada de galería, con campos Marca, Dispositivo, Función/tipo,
  Descripción y Autor (opcional) — igual que una entrada del catálogo.
- Las guardadas aparecen en el SELECTOR DE DISPOSITIVOS de arriba, bajo
  una marca nueva "⭐ Mis guardados": se navega Marca → Dispositivo
  (marca — dispositivo) → Función igual que cualquier device, se ve su
  descripción y autor, y se cargan con "Usar esta configuración". Siguen
  gestionándose (cargar/borrar) desde la lista de la tarjeta.
- El campo Marca tiene sugerencias (datalist) con las marcas del catálogo.
- Migración automática del formato anterior (v1: solo nombre) al nuevo
  (v2: marca/dispositivo/función/descripción/autor). Sigue siendo 100%
  local en el navegador; nada se sube. Bilingüe ES/EN.
- Ficheros: index.html (formulario), app.js (formato v2 + integración en
  el selector), i18n.js (textos), style.css (formulario/lista). Catálogo
  sin cambios (234).

Cambios desde v1.43:
- "Pegar JSON existente" ahora detecta automáticamente si el JSON pegado
  es de una versión anterior a v12 (notación objeto {"g":..} en las
  acciones y/o sin "io" central) y, al pulsar "Cargar en el formulario",
  lo convierte a v12 antes de cargarlo (reutiliza convertLegacyToV12) y
  muestra un aviso ⚠ en color ámbar indicando que era formato antiguo y
  se ha convertido. Si el JSON ya es v12, se carga normal sin aviso.
  Antes había que pasar por el convertidor a mano; ahora el "Cargar"
  también convierte. Ficheros: app.js (loadJsonIntoForm), i18n.js
  (loadJsonConverted ES/EN), style.css (#load-status.warn). Bilingüe.

Cambios desde v1.42:
- 3 dispositivos nuevos aportados por la comunidad (Discord), catálogo
  ahora con 234 entradas:
  · "Espejo retroiluminado con sensor de presencia (HLK-LD2410B)" (Otros):
    luz LED (t:30) que se enciende/apaga con un sensor de presencia por
    microondas HLK-LD2410B (accesorio de ocupación t:6). Entradas f0/f1
    convertidas de notación objeto a array v12.
  · "Kincony KC868-A4" → 2º ejemplo "Acuario": 3 termómetros DS18B20
    (agua/aire/sump), 4 relés como válvulas (bomba sump, aireador, luces
    UV, ósmosis) con temporizadores, y 2 sensores de contacto que accionan
    relés. Config ya en array v12.
  · "Ilonda (alimentador de acuario)" (Otros): interruptor (t:1) que
    acciona el motor del alimentador, con botón y LED. Config ya en array.

Cambios desde v1.41:
- NUEVA FUNCIÓN: "Mis configuraciones guardadas". Nueva tarjeta bajo
  "JSON generado" que permite guardar la configuración actual con un
  nombre en el propio navegador (localStorage) y recuperarla más tarde:
  · Campo de nombre + "Guardar la actual" (guarda el JSON que produce el
    formulario en ese momento). Si el nombre ya existe, lo actualiza.
  · Lista de configuraciones guardadas con botones "Cargar" (rellena el
    formulario y salta a la vista avanzada) y "Borrar" (con confirmación).
  · Persiste al cerrar/recargar. Es 100% local en el dispositivo del
    usuario: no hay servidor ni se sube nada (la web es estática). Se
    avisa de ello en la interfaz, y de que borrar los datos del navegador
    la elimina.
  Bilingüe ES/EN. Ficheros: index.html (tarjeta), app.js (funciones
  getSavedConfigs/saveCurrentConfig/renderSavedList + wiring), i18n.js
  (textos), style.css (estilos de la lista). Catálogo sin cambios (231).

Cambios desde v1.40:
- NUEVA FUNCIÓN: convertidor de MEPLHAA antigua (v11 "Peregrine") al
  formato actual (v12 "Merlin"). Nueva tarjeta en el panel derecho, bajo
  "Pegar JSON existente". Pegas una config antigua y:
  · Convierte las acciones de relé "r" de notación objeto
    {"g":..,"v":..,"i":..} a array posicional [g,v,i].
  · Convierte los botones "b"/"f[n]" de {"g":..,"t":..} a [g,t].
  · Reconstruye el "io" central (obligatorio en v12) a partir de los GPIOs
    usados: salidas (las de "r" + el LED "l") como modo 2, entradas (las de
    "b"/"f[n]") como modo 6. Si el config ya trae "io", se respeta.
  · Botones: "Convertir a v12", "Copiar resultado" y "Cargar resultado en
    el formulario" (lo carga y salta al formulario avanzado).
  Es idempotente (aplicarlo sobre un config ya v12 no lo estropea) y no
  mete los pines de sensores en el "io". Aviso en la interfaz: los botones
  se ponen como entrada simple (modo 6); si un botón físico necesita
  pull-up hay que ajustarlo a mano (6,1). Bilingüe ES/EN.
- Ficheros modificados: app.js (convertLegacyToV12 + wiring), index.html
  (tarjeta nueva), i18n.js (textos ES/EN). Sin cambios en el catálogo
  (sigue en 231 dispositivos).

Cambios desde v1.39:
- Tanda de 6 dispositivos nuevos aportados por rodolfo_homekit (comunidad
  vía Discord #meplhaa-configurator-es), catálogo ahora con 231 entradas:
  · "Shelly 1 (ESP8266)" → "Abre-portero / telefonillo del portal": abre
    el portero automático (cerradura t:4, pulso de relé 3 s en GPIO 4).
  · "Shelly 1 (ESP8266)" → "Alarma para trastero": sistema de seguridad
    (t:55) con sensor de contacto cableado, armado con retardo de 5 min,
    sirena 180 s en el relé y avisos push insistentes.
  · "Shelly 2.5 (ESP8266)" → "Ventilación de baño por humedad, 2 vel.":
    ventilador t:65 de 2 velocidades según humedad (65 %/80 %), auto-off
    900 s, con medidores de potencia y termómetro.
  · "Shelly Plug S (ESP8266)" → "Enchufe con auto-apagado de seguridad":
    apagado por tiempo máximo (d) como protección (nebulizador). ⚠ La
    config trae d:7200 (2 h); el autor lo citó como ~1800 s — ajustar.
  · "NodeMCU (ESP8266)" (Otros) → "Anemómetro para protección de toldo":
    monitor libre (t:80) que mide el viento y, al pasar el umbral (v:70),
    dispara la recogida del toldo vía un sensor de movimiento "falso".
  · "Sinos iFan" (Otros) → "Ventilador de techo 3 velocidades con mando":
    ventilador t:65 (3 vel.) + receptor de mando a distancia (t:85).
- Adaptaciones: donde hacía falta, acciones en notación objeto {"g":..}
  convertidas a array v12 (mismos GPIOs/valores); quitadas etiquetas
  personales de nivel c ("n":"ventilacion_bano", "n":"nebulizador"). Las
  calibraciones (vf/vo/cf/co/pf) se conservan tal cual (son del autor).
- Nota: la aportación repetida del "Shelly Plug S / Hubs (arranca en ON)"
  ya se había añadido en v1.39, no se duplicó.

Cambios desde v1.38:
- Nuevo ejemplo aportado por rodolfo_homekit (comunidad), catálogo ahora
  con 225 entradas: "Shelly Plug S (ESP8266)" → "Enchufe que arranca en
  ON (recupera tras cortes/reconexión)". Enchufe (t:2) con estado inicial
  ENCENDIDO (s:1): tras corte de luz/reinicio/pérdida de conexión vuelve a
  arrancar en ON (útil para hubs/routers). Relé en GPIO 15 (GPIO 0 en
  oposición = LED azul), botón GPIO 13, LED GPIO 2. Sin medidor de
  potencia (a diferencia del otro ejemplo de Shelly Plug S).
- Adaptaciones sobre la config aportada: acciones en notación objeto
  {"g":..} convertidas a array v12 (mismos GPIOs/valores); corregida una
  errata evidente ("cg":15 -> "g":15 en la acción de encendido, que si no
  dejaría el relé sin activarse); y quitada una etiqueta personal
  "n":"Hubs".

Cambios desde v1.37:
- Nuevo ejemplo aportado por rodolfo_homekit (comunidad), catálogo ahora
  con 224 entradas: "Electrodragon placa de relé SPDT" → "Estor/persiana
  (rodolfo_homekit, con guía de conexión, sin LED)". Persiana/estor con
  relés en GPIO 12/13, botones subir/bajar/parar en GPIO 4/5, botón de
  configuración en GPIO 2, sin LED de estado (tiempos 18/14 s). Incluye
  la guía de conexión del autor: GND a la L del interruptor, IO4/IO5 a
  subida/bajada, y ⚠ aviso de no aplicar 220 V al interruptor (son
  entradas de señal). Config ya en formato array v12, usada tal cual.
  Es casi idéntica al ejemplo de persiana Electrodragon que ya existía
  (segundo ejemplo del mismo modelo); se mantiene por el crédito y la
  guía de conexión.

Cambios desde v1.36:
- Nuevo dispositivo aportado por la comunidad vía Discord
  (#meplhaa-configurator-es), catálogo ahora con 223 entradas:
  · "Kincony KC868-A4 (ESP32)" (Otros): placa de relés para piscina —
    4 electroválvulas de agua (t:20, relés GPIO 15/2/5/4, máx. 4 h),
    2 termómetros DS18B20 en el mismo bus (GPIO 13, el 2º con u:2),
    sensor de puerta (GPIO 27) y sensor de nivel de agua (GPIO 36) que
    acciona la válvula del relé 5 (llenado). Config ya en formato array
    v12, usada tal cual. ⚠ La KC868-A4 tiene varias versiones de
    hardware (algunas con expansor I2C): se marca en el nombre y en la
    descripción que hay que verificar los GPIOs de la unidad concreta.
    Nota: el JSON trae "n":"Piscina" a nivel raíz (etiqueta no estándar
    que el configurador ignora/descarta al regenerar).

Cambios desde v1.35:
- Tres dispositivos nuevos aportados por la comunidad vía Discord
  (#meplhaa-configurator-es), catálogo ahora con 222 entradas:
  · "Sonoff T2 EU" (Sonoff): interruptor táctil de pared 2 canales usado
    como persiana/toldo (relés GPIO 12/5, botones 0/9, LED en drenaje
    abierto GPIO 13). Mismo mapa de GPIOs que el Sonoff 2CH.
  · "Sonoff Dual R3 v1.6" → nuevo ejemplo "Toldo/Persiana con
    interruptores externos y protección 1A" (junto al ejemplo de persiana
    Dual R3 que ya existía, no lo reemplaza): interruptores externos GPIO
    32/33, medidor de potencia, protección por sobrecorriente ~1 A,
    cruce por cero en GPIO 4, UART a 4800. Alternativa más simple a la
    versión con la init completa del CSE7761.
  · "Eachen IR DC6" (Otros): controlador IR para aire acondicionado LG
    como HeaterCooler (t:25) con códigos IR aprendidos + termómetro
    (t:24). ⚠ Los códigos IR son específicos de ese equipo/mando; el
    accesorio IR se carga en el campo JSON avanzado (t:25 sin formulario
    dedicado).
- Se descartó una cuarta aportación ("Sonoff Dual persianas") por ser
  idéntica a la entrada Sonoff 2CH → Toldo/Persiana ya existente salvo
  los tiempos (decisión del mantenedor).
- El T2 EU y el Dual R3 "actual" ya venían en formato array v12, se
  usaron tal cual. La notación objeto {"g":..} solo estaba en la
  aportación descartada (Sonoff Dual) y en una acción del Eachen IR, que
  se conservó verbatim al ser IR (sin formulario dedicado).

Cambios desde v1.34:
- Nuevo dispositivo en el catálogo (219 entradas): "eWeLink RMC021"
  (categoría Otros), aportado por la comunidad vía Discord
  (#meplhaa-configurator-es). Enchufe/relé con medición de consumo para
  lavadora y lavavajillas que detecta el fin del ciclo por la caída de
  potencia y lo refleja en un sensor de contacto (relé biestable en GPIO
  13/14, botón en GPIO 3, medidor en dt:[5,4,12] con calibración
  vf/cf/pf). El pinout de medida y los factores cf/pf coinciden con los
  del Gosund SP111 (hardware muy similar), lo que cross-valida el medidor.
- La config aportada venía con las acciones de relé/botón en notación
  objeto {"g":..,"v":..} (estilo v11 "Peregrine"); el firmware v12 la
  puede aceptar, pero el formulario del configurador solo reconoce relés
  y botones en formato array (app.js: Array.isArray). Se ha convertido a
  formato array v12 ([14],[13,1],[3,5]...) conservando exactamente los
  mismos GPIOs y valores, para que el formulario se rellene bien.
- Nota de honestidad de datos: los GPIOs y la calibración provienen de
  una config real de un usuario, no de la documentación oficial de
  RavenSystem; "RMC021" es el nombre indicado por quien la aportó, y la
  calibración vf/cf/pf es específica de cada unidad (recalibrar por
  aparato). Todo ello queda explicado en la descripción bilingüe.

Cambios desde v1.33:
- Catálogo bilingüe completo: las 218 entradas (nombre de ejemplo y
  descripción) ahora tienen traducción al inglés (exampleEn/descriptionEn
  en devices.js). Al cambiar el idioma de la web a inglés, el selector de
  dispositivos (categoría, ejemplo) y el texto explicativo cambian de
  idioma correctamente, sin perder la selección que tuvieras hecha. Las
  ~120 configuraciones migradas del issue #689 ya tenían el nombre de
  ejemplo en inglés de origen; solo les faltaba traducir la descripción
  ("Ejemplo de la comunidad..."). Las ~98 entradas restantes (oficiales
  de la wiki + aportaciones de la comunidad de este proyecto) se han
  traducido de cero.

Cambios desde v1.32:
- Añadido tercer ejemplo de comunidad (rodolfo_homekit): Shelly 1
  controlando una electroválvula N/A de suministro de agua, con lógica
  invertida a propósito, cierre automático programado a las 23:00,
  histórico de aperturas/cierres, e interruptor virtual de bloqueo de
  seguridad para evitar que la automatización horaria reabra el agua si
  hay una fuga. Este JSON ya venía en formato v12 correcto, sin
  necesidad de conversión. Catálogo: 217 → 218 dispositivos.

Cambios desde v1.31:
- La descripción del dispositivo/ejemplo elegido ya no se muestra como una
  línea pequeña de "hint" — ahora se ve como texto legible, en párrafos
  separados (uno por cada salto de línea doble en la descripción), y
  respeta saltos de línea sueltos para viñetas. Se han reestructurado en
  párrafos las dos descripciones largas de la comunidad (Shelly 1PM
  Alarma y Shelly EM) para aprovecharlo.

Cambios desde v1.30:
- Añadido segundo ejemplo de comunidad para el Shelly EM (rodolfo_homekit):
  medidor de doble canal con historial diario, alerta de consumo a 4400W
  vía sensor de contacto, y reinicio mensual del contador. El JSON
  original no incluía el array "io" — se completó cruzando dos fuentes
  independientes (los datos de rodolfo y la ficha de Blakadder para el
  Shelly EM clásico), y coincide exactamente con el "io" del ejemplo
  básico que ya teníamos en el catálogo (sourced de la wiki oficial),
  lo que confirma que es correcto. Catálogo: 216 → 217 dispositivos.

Cambios desde v1.29:
- Añadido al catálogo un ejemplo aportado por la comunidad (rodolfo_homekit):
  Shelly 1PM como Sistema de seguridad, con sensor de contacto cableado
  para la puerta y un interruptor virtual para automatizaciones por
  geolocalización. Convertido de formato v11 a v12 (el original usaba
  sintaxis antigua de objetos para los relés/botones). Pendiente de subir
  a la web publicada — hay más ejemplos en camino, se subirán juntos.

Cambios desde v1.28:
- Corregidos dos bugs reportados por RavenSystem al crear un dispositivo
  personalizado desde el asistente: (1) el estado inicial declaraba un
  GPIO 12 de ejemplo desde el principio, sin usarse ni avisar — ahora
  empieza vacío. (2) Escribir un GPIO en un campo del asistente (LED,
  salida del relé, botón...) y luego borrarlo o cambiarlo dejaba el GPIO
  anterior declarado para siempre como "fantasma", sin ningún accesorio
  que lo usara. Ahora cada campo limpia su propia declaración anterior
  antes de añadir la nueva, así que borrar/cambiar el valor no deja
  restos.

Cambios desde v1.27:
- Cada tarjeta de accesorio en el formulario avanzado ahora tiene un
  encabezado ("Accesorio #3 — 75 - Medidor de potencia") con su número y
  tipo. Antes, al cargar una configuración con varios accesorios, había
  que abrir el desplegable "Tipo" de cada tarjeta una a una para saber
  cuál era cuál — por ejemplo, para encontrar el medidor de potencia
  entre varios interruptores/sensores.

Cambios desde v1.26:
- Añadido un aviso de la comunidad (mcalve) en las 2 entradas del Shelly
  Plus 2PM: hay muchas versiones de hardware distintas según el mes/año
  de fabricación, así que conviene verificar que los GPIOs coinciden con
  la unidad real antes de usar la configuración.

Cambios desde v1.25:
- Añadido un enlace visible en la cabecera de la web ("💡 Sugerencias y
  errores: repositorio en GitHub"), a petición de RavenSystem, para que
  cualquiera pueda encontrar fácilmente dónde reportar fallos o proponer
  mejoras.

Cambios desde v1.24:
- Corregido: al pegar un JSON en el cuadro "Pegar JSON existente" estando
  en modo Asistente, parecía que "no pasaba nada" — el JSON de la derecha
  sí se actualizaba, pero la lista de GPIOs/accesorios resultante (en el
  formulario avanzado) quedaba oculta. Ahora, igual que ya pasaba al
  elegir un dispositivo del catálogo (v1.18), pegar un JSON válido cambia
  automáticamente al formulario avanzado para que se vea el resultado de
  golpe. Si el JSON no es válido, no cambia de modo.

Cambios desde v1.23:
- Actualizada la entrada "eWeLink EU 20A Plug AJW-02" con datos reales
  aportados por un usuario: calibración auténtica del medidor de potencia
  (antes tenía el valor de partida genérico sin calibrar) y el LED
  (GPIO13) ahora sincronizado con el estado del relé, tipo de accesorio
  correcto (Enchufe). Ya no lleva el aviso de "sin calibrar".

Cambios desde v1.22:
- Añadida una sección "Cómo aportar un dispositivo al catálogo" al
  README, con instrucciones tanto para quien solo quiera sugerirlo (sin
  programar) como para quien quiera añadirlo directamente por Pull
  Request, incluyendo las reglas para no inventar GPIOs/calibraciones.
- Añadida una plantilla de issue de GitHub ("🔌 Sugerir un dispositivo",
  en .github/ISSUE_TEMPLATE/) con los campos necesarios (marca, modelo,
  chip, enlace a la ficha técnica, GPIOs si se conocen, uso previsto).
  No afecta a la web en sí, solo al repositorio de GitHub.

Cambios desde v1.21:
- El JSON generado se muestra ahora en una sola línea por defecto (ocupa
  menos espacio en la vista principal); el botón para pasar a formato
  legible sigue disponible.
- Revisados los 18 dispositivos del primer lote de Blakadder: 11 tenían
  algún GPIO real del dispositivo (normalmente un segundo LED indicador)
  que no se había declarado. Se han añadido todos — Athom PG01V3 (GPIO20,
  pin del medidor CSE7766, declarado como entrada aunque su protocolo no
  está documentado para HAA), Athom SW011EU/SW031US, BlitzWolf SHP2/SHP6,
  Gosund SW1/SW3, Teckin SR40 (2 LEDs), Nous L1T, LSC Smart Connect
  3202087 y Avatto TS02-1 (2 LEDs). No cambia el número de dispositivos
  del catálogo, solo se completan sus configuraciones.

Cambios desde v1.20:
- Corregido: 5 enchufes con chip de medidor de potencia BL0937 (BlitzWolf
  SHP2/SHP6-15A, Gosund WP1, Nous A1T, LSC Smart Connect 3202087) y el
  eWeLink AJW-02 se habían dejado sin el medidor configurado por no tener
  valores de calibración documentados. Se ha encontrado que la wiki de
  RavenSystem (página "Power-Monitor") sí publica un valor de partida
  oficial antes de calibrar ({"vf":0.1,"cf":0.1,"pf":1}), así que ahora
  los 6 sí incluyen el medidor de potencia (sin calibrar, con aviso en la
  descripción de que hay que calibrarlo con una carga conocida para
  lecturas exactas).

Cambios desde v1.19:
- 6 dispositivos eWeLink/EARU más al catálogo (ESP8266/ESP8285
  confirmado, GPIO real): interruptor 1 canal sin neutro, módulos de 3 y
  4 relés (PSF-B04), enchufe 20A AJW-02 (sin medidor de potencia por no
  haber calibración documentada), y 2 interruptores automáticos DIN
  (RDCBC-1P/2P). Catálogo: 209 → 215 dispositivos.

Cambios desde v1.18:
- Primer lote de ampliación del catálogo desde templates.blakadder.com:
  18 dispositivos nuevos en "Otros" (Athom, BlitzWolf, Gosund, Teckin,
  Nous, LSC, Avatto), todos con chip ESP8266/ESP32 confirmado y tabla de
  GPIO real verificada. Solo se configura relé+botón+LED (patrón ya
  probado en el resto del catálogo); donde el dispositivo tiene chip de
  medidor de potencia (BL0937/CSE7766) pero no hay valores de calibración
  documentados para HAA, se deja sin configurar en vez de inventarlos —
  se indica en la descripción de cada uno.
  Descartados de este lote por riesgo real (no boletos a la ligera):
  Teckin SP10 y SP22 (la propia página avisa de unidades con chip no-ESP
  en producción reciente) y Nous B1T/B2T/A5T (los datos de GPIO extraídos
  eran genéricos/no fiables, no específicos del dispositivo).
  Catálogo: 191 → 209 dispositivos.

Cambios desde v1.17:
- Corregido: tras separar el asistente del formulario avanzado (v1.17), al
  elegir un ejemplo del catálogo la web se quedaba en modo "asistente",
  que solo pregunta paso a paso — así que la lista de GPIOs y accesorios
  ya rellenados (en el formulario avanzado) quedaba oculta y parecía que
  "la configuración de ejemplo había desaparecido". Ahora, al cargar un
  ejemplo real del catálogo, se muestra directamente el formulario
  avanzado (donde se ve todo de golpe). La opción "Personalizado (GPIOs
  de este modelo...)" sigue llevando al asistente, ya que ahí no hay
  accesorios predefinidos que mostrar.

Cambios desde v1.16:
- Cambio de comportamiento: el asistente y el formulario avanzado ya NO
  se muestran juntos en la misma pantalla (esto generaba confusión). Al
  elegir uno de los dos botones de arriba ("Ir al asistente" / "Ir al
  formulario avanzado"), se oculta el otro por completo; el botón activo
  queda resaltado. El estado sigue siendo el mismo en ambos modos: cambiar
  de uno a otro no pierde nada de lo ya configurado.

Cambios desde v1.15:
- Añadido "Toldo/Persiana" para el Shelly Plus 2PM, marcado claramente
  como ⚠ adaptación no oficial (la wiki de RavenSystem no lo documenta
  todavía, solo dice "TO-DO"). Se ha construido reutilizando los GPIOs
  reales de este modelo (ya verificados) y el mismo patrón de conmutación
  con detección de cruce por cero que usan otros dispositivos con
  persiana ya documentados oficialmente (Sonoff Dual R3), sin inventar
  valores de calibración de medidor de potencia para este uso. Se avisa
  en la descripción para que se pruebe con cuidado antes de confiar en él.

Cambios desde v1.14:
- Añadidos al catálogo, a petición de la comunidad (mcalve): Shelly Plus
  2PM (2 interruptores con medidor de potencia y termómetro) y Sonoff
  Dual R3 v1.6 (2 interruptores con medidor de potencia, y Toldo/Persiana
  con medidor de potencia). Sacados literalmente de las páginas oficiales
  de la wiki de RavenSystem para cada dispositivo, sin modificar. El
  ejemplo de "Toldo/Persiana" del Shelly Plus 2PM no se incluye porque la
  wiki lo marca como "TO-DO" (aún no documentado oficialmente).

Cambios desde v1.13:
- El aviso de "dispositivo detectado/indicado" ahora enlaza también a
  Blakadder (templates.blakadder.com), que es donde está la ficha de
  pinout/esquema por modelo exacto (foto + tabla de GPIOs). Como no se
  puede garantizar la URL exacta de cada uno de los ~80 modelos del
  catálogo sin comprobarlos uno a uno, se usa un enlace de búsqueda con el
  nombre del modelo en vez de adivinar la dirección — así nunca lleva a
  una página equivocada. El enlace a la wiki de RavenSystem se mantiene
  como información adicional (documentación del propio firmware HAA).

Cambios desde v1.12:
- Corregido: el número de versión (footer) estaba al final del todo de la
  página, había que hacer mucho scroll para verlo. Ahora se muestra junto
  al título, arriba, visible sin hacer scroll.

Cambios desde v1.11:
- Al cargar o pegar un JSON, la web ahora intenta identificar de qué
  dispositivo se trata comparándolo con el catálogo (ignorando hostname y
  nombres libres). Si lo reconoce, muestra un aviso en "Configuración
  General" con el dispositivo detectado y un enlace a su ficha en la wiki
  de RavenSystem (fotos/pinout). Si no lo reconoce (dispositivo propio o
  fuera del catálogo), aparece un selector opcional Fabricante→Modelo para
  indicarlo tú mismo — no cambia la configuración, solo permite mostrar el
  enlace correspondiente. Lo mismo está disponible en el primer paso del
  asistente, sincronizado con el formulario avanzado.

Cambios desde v1.10:
- Añadido un pie de página visible en la propia web ("Configurador
  MEPLHAA vX.X") para poder comprobar de un vistazo qué versión está
  publicada, sin tener que mirar el código fuente ni el archivo de red.

Cambios desde v1.9:
- Corregido: la sugerencia de reparación automática de JSON (v1.8) no
  detectaba todos los casos de llaves/corchetes desbalanceados — solo
  miraba si el texto empezaba por "{" y terminaba en "}", así que un JSON
  al que le faltaran los cierres "]}" del final (aunque terminase en "}"
  de un nivel más interno) no se reparaba. Ahora se analiza el balance
  real de "{"/"}"/"["/"]" (ignorando el contenido de las cadenas) para
  saber exactamente qué apertura falta al principio y qué cierre falta al
  final, cubriendo casos más complejos como un dispositivo con varios
  accesorios y servicios extra anidados al que le faltase el cierre final.

Cambios desde v1.8:
- Añadida analítica de visitas con GoatCounter (script en index.html,
  sitio "meplhaa.goatcounter.com"). No cambia nada visible ni de
  comportamiento para el usuario; solo cuenta visitas de forma anónima
  y sin cookies.

Cambios desde v1.7:
- Al pegar un JSON con errores de sintaxis (llave { o } de apertura/cierre
  que falta, coma final sobrante...), la web ahora intenta detectar y
  reparar el problema automáticamente. Si encuentra una corrección válida,
  la muestra en una caja con vista previa y dos botones: "Usar esta
  corrección" (la aplica y carga el formulario) o "Descartar". Si el JSON
  está demasiado roto para repararlo automáticamente, se mantiene el
  mensaje de error de siempre sin sugerir nada.

Cambios desde v1.6:
- Añadido README.md con instrucciones de uso completas (cómo arrancarla,
  qué hace cada sección, estructura del proyecto y referencias). No hay
  cambios funcionales en la app respecto a v1.6 (pasada de QA completa
  sin encontrar fallos: regresión de las 187 configuraciones del catálogo,
  auto-separación de servicios extra, asistente, vista móvil e idioma
  ES/EN, todo correcto).

Cambios desde v1.5:
- Al cargar un dispositivo (o pegar un JSON) que agrupa varios relés/válvulas
  como servicios extra ("es"), ahora se SEPARAN AUTOMÁTICAMENTE en tarjetas
  de accesorio independientes desde el primer momento — ya no hace falta
  pulsar ningún botón. "Sonoff 4CH — Riego" ahora muestra directamente sus
  4 válvulas, cada una con su propio GPIO editable.
  El botón "🔓 Separar en accesorios independientes" se mantiene como
  respaldo para cuando pegues o edites tú mismo un JSON con "es" a mano.
  Nota: esto significa que esos accesorios pasan a mostrarse como tarjetas
  independientes en la app Home (antes compartían una sola tarjeta).

Cambios desde v1.4:
- Corregido: la detección de "Relé"/"Botón" solo miraba el accesorio
  principal y no los servicios extra ("es") agrupados dentro de él. Ahora
  se recorre TODO el JSON del accesorio, así que si un dispositivo tiene
  4 relés (o más) agrupados, los 4 se marcan correctamente como "Relé".

Cambios desde v1.3:
- (Sustituido por el cambio automático de v1.6 — el botón manual se
  mantiene disponible para ediciones futuras).

Cambios desde v1.2:
- Cada fila de "GPIOs (io)" muestra una etiqueta automática (Relé/LED/Botón).

Cambios desde v1.1:
- Una fila por GPIO en vez de agrupados por coma.

Cambios desde v1.0:
- Botón "Una sola línea" para el JSON compacto.
- Corregido el texto de los botones-interruptor al cambiar de idioma.

Resumen general (acumulado):
- Formulario avanzado: Configuración General, GPIOs ("io") con modo + pull-up/down
  + parámetros (una fila por GPIO, con etiqueta Relé/LED/Botón), Accesorios
  con campos dedicados para Interruptor/Enchufe, Botón sin estado, Cerradura,
  Válvula de agua, Termómetro/Humedad, Bombilla — los servicios extra
  agrupados se separan automáticamente en tarjetas editables individuales
  al cargar cualquier dispositivo o JSON pegado.
- Panel "¿Qué se puede configurar en este tipo?" por tipo de accesorio (34 tipos).
- Asistente guiado paso a paso (mismo motor que el formulario avanzado).
- Selector de dispositivos en 3 niveles: Fabricante -> Modelo -> Ejemplo/función,
  con categoría "Personalizado" que además permite explorar TODOS los ejemplos
  del catálogo en un único desplegable y autocompleta fabricante/modelo al elegir uno.
- Catálogo de 187 configuraciones: dispositivos oficiales de la wiki de RavenSystem
  (Shelly, Sonoff, Otros) + 120 migrados automáticamente de v11 a v12 desde el
  issue #689 de GitHub (community collection).
- Selector de idioma ES/EN para toda la interfaz interactiva.
- Pegar JSON existente para rellenar el formulario.
- JSON generado en vivo (legible o en una línea), con avisos de validación.

Cómo usarla:
1. Descomprime el zip.
2. Abre una terminal en esa carpeta y ejecuta: python -m http.server 8096
3. Abre http://localhost:8096 en el navegador.
(O usa cualquier otro servidor estático; son ficheros HTML/CSS/JS puros, sin build).
